# Google Sheets order logging (optional — checkout works fine without these,
# it just won't log orders to a Sheet). See setup steps from Claude/the README.
GOOGLE_SHEETS_WEBHOOK_URL=
GOOGLE_SHEETS_SECRET=
